# 🖱️ JSCursor

**JSCursor** — это кастомный React-компонент курсора, который плавно следует за мышью, деформируется при движении, исчезает за пределами окна и реагирует на клики и hover-события.

---

## 🚀 Возможности

- ✅ Плавное движение и инерция  
- ✅ Реалистичная деформация при быстром движении  
- ✅ Исчезает при выходе курсора за пределы окна  
- ✅ Реагирует на клики (уменьшение на 4px)  
- ✅ Подсвечивается при наведении на элементы с классом (по умолчанию `.ActiveHover`)  
- ✅ Настройка через пропсы при подключении в `App.jsx`  
- ✅ Написан на **React + чистом CSS**

---

## ⚙️ Настройка

Компонент можно настраивать через пропсы:

| Пропс | Тип | Значение по умолчанию | Описание |
|-------|-----|----------------------|----------|
| `deformationEffect` | `boolean` | `false` | Эффект деформации при движении мыши |
| `clickEffect` | `boolean` | `false` | Эффект уменьшения курсора при клике |
| `hiddenCursor` | `boolean` | `false` | Исчезновение курсора при выходе за пределы окна |
| `classHover` | `string` | `"ActiveHover"` | Класс элементов, при наведении которых курсор изменяет вид |

**Пример использования:**

```jsx
<JSCursor 
  deformationEffect={true}   
  clickEffect={true}         
  hiddenCursor={true}       
  classHover="ActiveHover"   
/>
```
---
## 🎨 CSS-настройка
Можно менять цвета курсора через CSS-переменные:
```css
:root {
  /* Цвет курсора по умолчанию */ 
  --defoult-cursor: #FF00C8; 
  /* Цвет границы курсора по умолчанию */ 
  --defoult-cursor-border: #FF00C8; 
  /* Цвет курсора при hover */ 
  --hover-cursor: #ffffff; 
  /* Цвет границы курсора при hover */ 
  --hover-cursor-border: #ff5fdc; 
}

```
---
## 🏞️ Демонстрация
`Плавное движение и инерция`

![Демо](./gif/movement-cursor.gif)

`Исчезает при выходе курсора за пределы окна`

![Демо](./gif/opacity-cursor.gif)

`Реагирует на клики`

![Демо](./gif/click-cursor.gif)

`Подсвечивается при наведении на элементы`

![Демо](./gif/hover-cursor.gif)

---
## 📦 Установка и использование
1. Скопируйте папку с компонентом в проект
2. Импортируйте компонент:
  ```jsx
  import JSCursor from "./components/Cursor/JSCursor";
  ```
3. Используйте компонент:
  ```jsx
  // Без настройки
  <JSCursor />
  // С настройкой (пример)
   <JSCursor 
          deformationEffect={true}   
          clickEffect={true}         
          hiddenCursor={true}       
          classHover="ActiveHover"   
        />
  ```
4. При необходимости настройте CSS файл `JSCursor.css`


